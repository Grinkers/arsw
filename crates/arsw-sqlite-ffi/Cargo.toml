[package]
name = "arsw-sqlite-ffi"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[features]
default = ["bundled-sqlite"]
bundled-sqlite = []

[dependencies]

[build-dependencies]
cc = "1.2"
pkg-config = "0.3"

[lints]
workspace = true
