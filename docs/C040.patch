diff --git a/tests/confirmation/C040.py b/tests/confirmation/C040.py
new file mode 100644
index 00000000..1880a91d
--- /dev/null
+++ b/tests/confirmation/C040.py
@@ -0,0 +1,20 @@
+"""C040 [HIGH] resetcursor can return success with an iterator exception pending
+
+CONFIRMED: static analysis (code path verified in source)
+Python 3.13.14.
+
+This test verifies the problematic code pattern exists in the current source
+tree. It does not execute the bug at runtime.
+"""
+from pathlib import Path
+
+def test():
+    src = Path('src/cursor.c').read_text()
+    needle = 'next = PyIter_Next(self->emiter)'
+    assert needle in src, f"Pattern not found: {needle} in src/cursor.c"
+    print(f"  src/cursor.c: OK - emiter probe in resetcursor can leave SQLITE_OK with pending exception")
+
+
+if __name__ == "__main__":
+    test()
+    print("CONFIRMED (static): C040")
