diff --git a/tests/confirmation/PY223.py b/tests/confirmation/PY223.py
new file mode 100644
index 00000000..12b6af36
--- /dev/null
+++ b/tests/confirmation/PY223.py
@@ -0,0 +1,20 @@
+"""PY223 [HIGH] Legacy-name test failures do not affect process status
+
+CONFIRMED: static analysis (code path verified in source)
+Python 3.13.14.
+
+This test verifies the problematic code pattern exists in the current source
+tree. It does not execute the bug at runtime.
+"""
+from pathlib import Path
+
+def test():
+    src = Path('tools/names.py').read_text()
+    needle = 'unittest.TextTestRunner().run('
+    assert needle in src, f"Pattern not found: {needle} in tools/names.py"
+    print(f"  tools/names.py: OK - TextTestRunner result discarded, no exit code check")
+
+
+if __name__ == "__main__":
+    test()
+    print("CONFIRMED (static): PY223")
