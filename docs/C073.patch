diff --git a/tests/confirmation/C073.py b/tests/confirmation/C073.py
new file mode 100644
index 00000000..ee7bd5cd
--- /dev/null
+++ b/tests/confirmation/C073.py
@@ -0,0 +1,20 @@
+"""C073 [HIGH] Builder bookkeeping failure leaves SQLite holding an untracked database pointer
+
+CONFIRMED: static analysis (code path verified in source)
+Python 3.13.14.
+
+This test verifies the problematic code pattern exists in the current source
+tree. It does not execute the bug at runtime.
+"""
+from pathlib import Path
+
+def test():
+    src = Path('src/session.c').read_text()
+    needle = 'sqlite3changegroup_schema'
+    assert needle in src, f"Pattern not found: {needle} in src/session.c"
+    print(f"  src/session.c: OK - post-schema bookkeeping failure leaves untracked db pointer")
+
+
+if __name__ == "__main__":
+    test()
+    print("CONFIRMED (static): C073")
