diff --git a/tests/confirmation/PY210.py b/tests/confirmation/PY210.py
new file mode 100644
index 00000000..8f953ffb
--- /dev/null
+++ b/tests/confirmation/PY210.py
@@ -0,0 +1,20 @@
+"""PY210 [HIGH] Generated JSON is decoded only for a discarded validation result
+
+CONFIRMED: static analysis (code path verified in source)
+Python 3.13.14.
+
+This test verifies the problematic code pattern exists in the current source
+tree. It does not execute the bug at runtime.
+"""
+from pathlib import Path
+
+def test():
+    src = Path('tools/json_bench.py').read_text()
+    needle = 'data.extend(items)'
+    assert needle in src, f"Pattern not found: {needle} in tools/json_bench.py"
+    print(f"  tools/json_bench.py: OK - data extended with original strings, not parsed objects")
+
+
+if __name__ == "__main__":
+    test()
+    print("CONFIRMED (static): PY210")
